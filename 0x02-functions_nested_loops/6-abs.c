#include "main.h"

/**
 * _abs - function to find the absolute value of a number
 * @i: function parameter
 * Return: -a or a
 */
int _abs(int i)
{
	if (i > 0)
return (i);
else if (i < 0)
return (-i);
else
return (0);
}
